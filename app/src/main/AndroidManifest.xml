<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.capitalroute">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".capitalRouteApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".addfavoritos"></activity>
        <activity android:name=".lugaresFavoritos" />
        <activity android:name=".Login" />
        <activity
            android:name=".menuMainPage"
            android:label="@string/title_activity_menu_main_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".menuHome"
            android:label="@string/title_activity_menu_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Home" />
        <activity android:name=".PermitirAccederUbicacion" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name=".cerrarsesionprueba" />
        <activity android:name=".olvidarContrasegna" />
        <activity android:name=".registrarUsuario" />
        <activity android:name=".pantallaBienvenida">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>